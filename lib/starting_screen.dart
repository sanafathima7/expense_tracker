/*

//import 'package:expense/expense.dart';
import 'package:flutter/material.dart';

class startScreen extends StatelessWidget {
  const startScreen(this.startExpense, {super.key});

  final void Funtion() startExpense;
  @override
  Widget build(context) {
    return Center(
        child: Column(
      mainAxisSize: MainAxisSize.min,
      children: [
        Image.asset(
          'assets/images/pic1.png',
          width: 300,
          //color: Color.fromARGB(147, 177, 177, 204),
        ),
        const SizedBox(height: 80),
        const Text(
          'start expense',
          style: TextStyle(
            color: Colors.white,
            fontSize: 24,
          ),
        ),
        const SizedBox(height: 30),
        OutlinedButton(
          onPressed: startExpense,
          child: const Text('Start'),
        ),
      ],
    ));
  }
}
*/
